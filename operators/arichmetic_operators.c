#include <stdio.h>
// i will be touching the ones that are new to me, others are just the same as python

int main()
{
    int d = 2;
    ++d;
    printf("%d changed\n", d);
    --d;
    printf("%d changed by -1", d);
}