#include <stdio.h>

const int NUMBER = 1;

/*
constants are values that will never change
your not allowed to change them after you created them
it is recomended to type them out in uppercase
*/
int main()
{
    printf("%i", NUMBER);
}